<nav>
  <% if user_signed_in? %>
    <h2><%= "Hello, #{current_user.email}!" %></h2>
    <p class="back" ><%= link_to 'Sair', destroy_user_session_path, method: :delete %></p>
  <% else %>
    <p><%= link_to 'Entrar', new_user_session_path %></p>
  <% end %>
</nav>
